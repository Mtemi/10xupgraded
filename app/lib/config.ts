// app/lib/config.ts
export const API_CONFIG = {
  // Update base URL to use relative path instead of absolute URL
  API_BASE: '/apa',
  WS_URL: window.location.origin // This will use the current domain
};

